import { ReactNode } from 'react';

import ResetPassword from '@/app/components/auth/reset-password/reset-password';

export default function ResetPasswordPage(): ReactNode {
  return <ResetPassword />;
}
